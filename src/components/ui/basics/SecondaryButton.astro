---
const { type, props } = Astro.props;
---
<button 
    type={type}
    class={`inline-flex shadow-md items-center justify-center px-4 py-2 rounded-md font-semibold text-xs uppercase tracking-widest 
        transition ease-in-out duration-150 focus:outline-none focus:ring-2 focus:ring-offset-2 border 
        text-neutral-500 border-neutral-500 hover:text-light-50 hover:bg-neutral-400 focus:bg-neutral-600 active:bg-neutral-700 focus:ring-neutral-400 dark:text-light-50 dark:border-warm-600 dark:hover:bg-warm-600 dark:focus:bg-warm-500 dark:active:bg-warm-400 dark:focus:ring-warm-300
        ${props}`}
>
    <slot />
</button>